# Copie para .env e preencha os valores. Nunca commite .env.

# API (opcional em dev)
PORT=3001

# Banco na Vercel (obrigatório para a API funcionar no deploy)
# Crie um banco em https://turso.tech e use a URL e o token aqui.
# Localmente não é necessário (usa SQLite em server/data).
# TURSO_DATABASE_URL=libsql://seu-db-seu-usuario.turso.io
# TURSO_AUTH_TOKEN=seu-token

# JWT para login (obrigatório em produção)
JWT_SECRET=uma-chave-secreta-longa-e-aleatoria

# Stripe (pagamentos)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...

# URL pública do site (para Stripe success/cancel e uploads)
# Local: http://localhost:8080  |  Vercel: https://seu-projeto.vercel.app
VITE_APP_URL=http://localhost:8080
PUBLIC_APP_URL=http://localhost:8080
